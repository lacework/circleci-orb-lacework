description: >
  Install the Lacework CLI (https://github.com/lacework/go-sdk/tree/master/cli)
steps:
  - run:
      name: Installing the lacework-cli
      command: |
        curl https://raw.githubusercontent.com/lacework/go-sdk/master/cli/install.sh | sudo bash
